:host {
    display: inline-block
    font-size: 12px
    white-space: nowrap
    position: relative

    min-width: 50px
    min-height: 50px
    overflow: hidden

    font-family: Helvetica,Arial,sans-serif
    color: #ddd
}
